#include <stdio.h>
#include <stdlib.h>
#include <math.h>
float Trigo(int x,int y);
int main()
{
    int a,b;
    printf("Insert adjacent and opposite:");
    scanf("%d:%d",&a,&b);
    printf("The hypotenuse is %f\n",Trigo(a,b));
    return 0;
}
float Trigo(x,y)
{
    return sqrt((x*x)+(y*y));
}
